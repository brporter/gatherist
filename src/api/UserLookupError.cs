namespace api;

public enum RecordError
{
    ConnectionFailed,
    NotFound
}

public enum UserError
{
    ConnectionFailed,
    NotFound
}

public enum TenantError
{
    ConnectionFailed,
    NotFound
}